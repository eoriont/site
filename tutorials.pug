---
layout: default.pug
title: Tutorials
subtitle: Learn from my tutorials.
---
-function truncate(str) {
  -return str.substring(0, 200) + "...";
-}
.cards
  each post in collections.tutorials
    a(href=post.url).card
      .cardheader
        h2= post.data.title
      .cardbody
        p= truncate(post.template.frontMatter.content)